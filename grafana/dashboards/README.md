# Grafana Dashboards

Place Grafana dashboard JSON files here to auto-provision them into Grafana using the provisioning provider in `./grafana/provisioning/dashboards/dashboards.yml`.

- To export a Dashboard from Grafana: Dashboard → Share → Export → Save JSON
- Put the JSON file here; Grafana will load it on start.
