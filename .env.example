# Это пример файла для переменных окружения.
# Скопируйте его в .env и замените значения на свои.
# ВАЖНО: Никогда не коммитьте этот файл с реальными секретами в git!

# Ключ авторизации Tailscale.
# Рекомендуется использовать ephemeral auth key с тегом, например, tag:monitoring.
# Создать ключ можно в админ-панели Tailscale: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-k...

# Slack Webhook URL для отправки уведомлений через Alertmanager
# Получите на https://api.slack.com/apps -> Create New App -> Incoming Webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Email конфигурация для критических алертов (Alertmanager)
ALERTMANAGER_EMAIL=your-monitoring@company.com
SMTP_PASSWORD=your-smtp-password-or-app-specific-token
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
